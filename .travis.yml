language: cpp
dist: bionic
compiler:
  - gcc
  - clang 

addons: 
  apt: 
    packages: 
      - libboost-all-dev

install: 
  # Qt5
  - sudo apt-get install -qq qt5-qmake qt5-default

script: 
  - qmake
  - make release
  - ./cpp_benchmark_add_one
